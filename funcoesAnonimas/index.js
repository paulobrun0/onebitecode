// console.log(doubleSpeed(50));
console.log(seedUp(60, 10));

let doubleSpeed = function (velocidade) {
  return velocidade * 2;
};

function seedUp(velocidade, aceleracao) {
  return velocidade + aceleracao;
}
